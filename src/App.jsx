import React from "react";
const App = () => {
  return(
    <div>App</div>
  )
}
export default App;
// This is a simple React component that returns a div containing the text "App".
// It uses ES6 arrow function syntax to define the component.
// The component is then exported as the default export of the module.